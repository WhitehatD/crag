# crag:auto-start
# Generated from governance.md by crag
# Regenerate: crag compile --target forgejo
name: Governance Gates

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  gates:
    name: Governance Gates
    runs-on: ubuntu-latest
    steps:
      - uses: https://code.forgejo.org/actions/checkout@v4
      - name: Setup Node.js
        uses: https://code.forgejo.org/actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm ci
      - name: "test: npm run test"
        run: |
          npm run test
      - name: "build: node --check bin/crag.js"
        run: |
          node --check bin/crag.js
      - name: "ci (inferred from workflow): node bin/crag.js help > /dev/null"
        run: |
          node bin/crag.js help > /dev/null
      - name: "ci (inferred from workflow): node bin/crag.js version"
        run: |
          node bin/crag.js version
      - name: "ci (inferred from workflow): node bin/crag.js analyze --dry-run > /dev/null"
        run: |
          node bin/crag.js analyze --dry-run > /dev/null
      - name: "ci (inferred from workflow): node bin/crag.js upgrade --check > /dev/null"
        run: |
          node bin/crag.js upgrade --check > /dev/null
      - name: "ci (inferred from workflow): node bin/crag.js workspace --json > /dev/null"
        run: |
          node bin/crag.js workspace --json > /dev/null

# crag:auto-end
